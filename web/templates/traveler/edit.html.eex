<h2>Edit traveler</h2>

<%= render "form.html", changeset: @changeset,
                        action: traveler_path(@conn, :update, @traveler) %>

<%= link "Back", to: traveler_path(@conn, :index) %>
